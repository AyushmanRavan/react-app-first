import React from 'react';
import {} from 'react-redux'

function FetchSagaData(props) {
    const dispatch = useDispatch()
    const { subscriber } = useSelector(state => state.subscriber)

    return (
        <div>
            
        </div>
    );
}

export default FetchSagaData;